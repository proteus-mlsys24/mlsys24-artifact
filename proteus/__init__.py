
from proteus.graph.ops import OperatorRegistry

# print(f"{len(OperatorRegistry.operators)} operators are found: {OperatorRegistry.operators}")

# for op in OperatorRegistry.operators:
#     print(op, OperatorRegistry.operator_lookup[op].num_inputs)